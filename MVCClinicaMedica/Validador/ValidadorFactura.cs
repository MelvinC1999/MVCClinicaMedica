using MVCClinicaMedica.Models;
using MVCClinicaMedica.Utilitario;

namespace MVCClinicaMedica.Validador
{
    public class ValidadorFactura
    {
        /*public void validaDatos(Factura factura)
        {
            if (factura != null)
            {
                if (factura.MontoTotal.Equals())
                {
                    throw new InvalidClientException("Identificación 0000000000 no válida");
                }

                if (cliente.Nombres.Length <= 2)
                {
                    throw new InvalidClientException("Nombre Incorrecto");
                }

                bool b = ValidadorCedula.VerificarCedula(cliente.Identificacion);
                Console.WriteLine("Validacion CI " + b);
                if (!b)
                {
                    throw new InvalidClientException("Identificación no válida");
                }


            }
        }*/
    }
}
