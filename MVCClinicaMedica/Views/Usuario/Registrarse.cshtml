@model MVCClinicaMedica.Models.UsuarioMedicoViewModel;
@{
    ViewData["Title"] = "Registro Usuario para Médico";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Registro Usuario para Doctor</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/Style.css" /> <!-- Enlace al archivo CSS externo -->
    <style>
        body {
            background-image: url('/img/doctor1.jpg');
            background-size: cover;
            background-repeat: no-repeat;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .card {
            background-color: rgba(255, 255, 255, 0.7); /* Fondo transparente */
        }
    </style>
</head>
<body>

    <div class="container">

        <div class="row justify-content-center">
            <div class="col-12 col-md-6 col-lg-4 text-center">

                <div class="card">
                    <div class="card-body text-center">
                        <h2 class="card-title">Crear Doctor</h2>
                        <form asp-controller="Usuario" asp-action="RegistrarDoctor" method="post" class="d-flex flex-column">

                            <div class="mb-3">
                                <input class="form-control" type="text" id="nombre" placeholder="Nombre" required asp-for="@Model.Medico.Nombre" />
                            </div>
                            <div class="mb-3">
                                <input class="form-control" type="text" id="apellido" placeholder="Apellido" required asp-for="@Model.Medico.Apellido" />
                            </div>
                            <div class="mb-3">
                                <input class="form-control" type="number" id="telefono" placeholder="Teléfono" required asp-for="@Model.Medico.Telefono" />
                            </div>
                            <div class="mb-3">
                                <input class="form-control" type="email" id="correoMedico" placeholder="Correo" required asp-for="@Model.Medico.Correo" />
                            </div>
                            <div class="mb-3">
                                <!--<label for="password" class="form-label">Contraseña</label>-->
                                <input class="form-control" type="password" id="password" placeholder="Contraseña" required asp-for="@Model.Usuario.Password" />
                            </div>

                            <div class="mb-3">
                           
                                @*<label for="horario" class="form-label">Especialidad</label>*@
                                <select class="form-control" id="horario" required asp-for="@Model.Medico.Horario">
                                    <option value="" disabled selected>Seleccione un horario</option>
                                    @foreach (var horario in ViewBag.Horarios)
                                    {
                                        <option value="@horario">@horario</option>
                                    }
                                </select>
                            </div>
                            <div class="mb-3">
                                @*<label for="especialidad" class="form-label">Especialidad</label>*@
                                <select class="form-control" id="especialidad" required asp-for="@Model.Medico.idEspecialidad">
                                    <option value="" disabled selected>Seleccione una especialidad</option>
                                    @foreach (var especialidad in ViewBag.Especialidades)
                                    {
                                        <option value="@especialidad.idEspecialidad">@especialidad.Descripcion</option>
                                    }
                                </select>
                            </div>
                            <div class="row justify-content-between">
                                <div class="col-md-6 mb-3">
                                    <a href="@Url.Action("Usuarios","Usuario")" class="btn btn-warning"><i class="fas fa-arrow-left"></i> Regresar</a>
                                </div>
                                <div class="col-md-6 mb-3 text-md-end">
                                    <button class="btn btn-primary" type="submit">Registrar</button>
                                </div>
                            </div>
                            @if (ViewData["Mensaje"] != null)
                            {
                                <div class="form-group">
                                    <div class="alert alert-danger" role="alert">
                                        @ViewData["Mensaje"]
                                    </div>
                                </div>
                            }
                        </form>

                    </div>

                </div>

            </div>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.min.js"></script>
</body>
</html>

